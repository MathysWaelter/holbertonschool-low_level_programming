#include "main.h"
#include <stdio.h>

/**
 * main - execute program
 * @argc: int of argument
 * @argv: char of argument
 * Return: always 0.
 */

int main(int argc, char **argv)
{
	printf("%s\n", argv[0]);

	return (0);
}
