#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	if (i == 0)
	{
		putchar('0');
	}
	if (I / 10 != 0)
		putchar(i % 10 + '0');

	return (0);
}
