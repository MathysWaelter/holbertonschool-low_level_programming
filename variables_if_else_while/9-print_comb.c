#include <stdio.h>
/**
  * main - Entry point
  *
  * Return: Always 0 (Success)
  */
int main(void)
{
	int x = 48;
	int b = ", "

	while (x != 58)
	{
		putchar(x);
		putchar(b);
		x++;
	}
	putchar('\n');
	return (0);
}
